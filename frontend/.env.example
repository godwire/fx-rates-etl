# URL of the FastAPI backend.
# Locally, this is the default already baked into src/api.js, so this
# file is only needed if you want to override it (e.g. for a deployed
# backend URL on Vercel/Netlify).
VITE_API_URL=http://localhost:8000
